# ロードマップ / 進捗管理

文書全体の入口は [ドキュメント案内](README.md)。設計判断の記録は [docs/discussion/](discussion/README.md)。

## 現在の姿

**2026-07-26 に全面的に作り直した。**やることは「動画のリストをとってきて投稿する」だけ。
削減の内訳と理由は [discussion/削減の記録](discussion/reductions.md)。

構成は [02 技術構成](spec/02-architecture.md) を参照。フラットな7ファイル。

| # | 項目 | 状態 |
|---|---|---|
| 1 | 設定読み込み + バリデーション（`config.py`） | ✅ 完了 |
| 2 | ボス名マッチと NG ワード（`classify.py`）+ 表形式テスト | ✅ 完了 |
| 3 | SQLite（`store.py`）: 投稿済み動画と収集期間の2テーブル | ✅ 完了 |
| 4 | Data API 検索（`youtube.py`）: `search.list` のみ | ✅ 完了 |
| 5 | 常駐ループ + 投稿 + スラッシュコマンド（`bot.py`） | ✅ 完了 |
| 6 | 起動（`cli.py`）とログ（`logging_setup.py`） | ✅ 完了 |

2026-07-26 の記録ではユニットテスト54件が通過。**Discord / YouTube への実接続は未検証**
（トークン・API キー・投稿先チャンネル ID の設定が必要）。

2026-07-26 の作り直し前の旧 DB スキーマとは互換性がない。経緯は [削減の記録](discussion/reductions.md) を参照。現在の DB を一律に削除する指示ではない。

## 実接続で確認したいこと

- [ ] `/start` の確認ボタンから収集が始まり、**開始の知らせが1通だけ**であること
- [ ] 動画が**ボスごとの正しいチャンネル**に投稿されること。判定不能なものが fallback へ行くこと
- [ ] タイトルと URL の2行投稿で、Discord が動画カードを展開すること
- [ ] `/stop` が収集件数サマリを出すこと（3秒の応答期限内に defer できているか）
- [ ] OR クエリの検索結果（5ボス分が 50 件の枠に収まるか、無関係な動画がどれだけ枠を食うか）
- [ ] 投稿ログ（`logs/bot.jsonl`）を見て、エイリアスの過不足を運用者と調整する
- [ ] NG ワードが効きすぎ / 効かなすぎでないか

## 運用開始前に必要な手作業

- [ ] **Discord にボス別チャンネル5本と fallback チャンネルを作成する**（Bot は作らない）
- [ ] `config/config.yaml` の `boss_channels` と `fallback_channel_id` に実 ID を設定
- [ ] `.env` に `DISCORD_BOT_TOKEN` / `YOUTUBE_API_KEY` を設定
- [x] 2026年9月の `config/bosses.yaml` を公式告知に基づいて更新（エイリアスは未追加）

## 2026-09-23: ドキュメント整理

- [x] 共通指示を `AGENTS.md` に集約し、`CLAUDE.md` は参照に統一
- [x] 文書全体の案内と作業別の参照先を `docs/README.md` に追加
- [x] 参照データの説明と分析結果を分離し、過去の記録の扱いを明記
- 確認: 文書のローカルリンクと差分を検証。Bot の実接続・参照データの再集計は未実施
- 既存の課題: `ruff check .` は元スクリプト `ytyt.py` の3件（I001 / E501 / SIM108）で失敗し、`ruff format --check .` も同ファイルの整形が必要。文書整理ではコードを変更していない

## 2026-09-23: 9月の参照動画一覧

- [x] 9月の5ボスを正式名で検索し、[HTML一覧](reference/priconne_cb_2026-09_full.html) と [取得データ](reference/priconne_cb_2026-09_search.json) を保存
- 取得結果: 延べ41件・ユニーク40件。関連は暫定で延べ33件・ユニーク32件。検索5回、各1ページで終了
- 確認: 保存データとHTMLの件数・動画ID・リンクの整合性。動画本編と実ブラウザー表示は未確認
